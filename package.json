{
  "name": "pega-tracer-event-perf-visualizer",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://vbarashko.github.io/pega-tracer-event-perf-visualizer",
  "dependencies": {
    "@ant-design/icons": "^5.3.0",
    "@babel/runtime": "^7.12.0",
    "antd": "^5.13.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "^3.0.1"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.12.0",
    "@babel/plugin-transform-react-jsx": "^7.12.0",
    "gh-pages": "^6.3.0"
  }
}
